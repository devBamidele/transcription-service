# LiveKit Configuration
# Get these from your LiveKit Cloud dashboard: https://cloud.livekit.io
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your_api_key_here
LIVEKIT_API_SECRET=your_api_secret_here

# Deepgram Configuration
# Get your API key from: https://console.deepgram.com/
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# Server Configuration
# Port for this transcription service (default: 3000, but 3001 to avoid conflicts)
PORT=3001

# Backend Configuration
# URL of your Nest.js backend API that will receive transcription analysis
BACKEND_URL=http://localhost:8080
# API key for authenticating with backend (service-to-service auth)
BACKEND_API_KEY=your-service-api-key-change-this

# JWT Authentication Configuration
# REQUIRED: Shared secret with backend for JWT token validation (min 32 characters recommended)
JWT_SECRET=your-super-secret-key-min-32-chars-change-this-in-production
# OPTIONAL: Expected token issuer (should match backend JWT issuer)
JWT_ISSUER=interview-backend
# OPTIONAL: Expected token audience (validates token is meant for this service)
JWT_AUDIENCE=transcription-service
